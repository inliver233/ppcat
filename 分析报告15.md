# 分析报告15 — cat-state单例dump(cs1含version/cs2稳定持久态) + 0xb9fc84未触发分析（test3 第十五轮）

> 承报告14 + 主控交接"cat-state静态线索+动态定位guard任务"。完成主控任务A(dump),中性措辞代码恢复。

---

## 〇、一句话

**dump到cat-state单例字段**: cs1(thread.field@0x80.field@0x1108)含"version/0.9.0"(应用配置,每次build新建), cs2(.field@0x25e0)稳定持久(压缩指针数组结构)。**0xb9fc84(cat-state函数)主界面未触发**(需进设置页每日喂喵/阅读页)。dump数据回传主控解读isHungry字段。

---

## 一、★ cat-state dump(spawn frida, 0xa52920入口读)

### 1.1 单例路径(主控给的, 验证可达)
```
thread(x26) = 0xffee84861380
thread.field@0x80 (singleton) = 0xffef1485dd20  (稳定, 3次HIT一致)
singleton.field@0x1108 (cs1) = 0xffeb00ee4fb9 → 0xffeb010e4ea9  (每次变, 新建对象)
singleton.field@0x25e0 (cs2) = 0xffeb000082c1  (稳定, 持久)
```

### 1.2 ★ cs1 dump(0x90) — 含version/0.9.0
```
024d0000 00000091 486400fe ffff01c9 0eef0098 00000000 0000a90c ef000402
51000000 00004180 00000600 00000000 00002600 00002600 00004180 00004025
500337ec 2280e000 00004180 00007665 7273696f 6e004180 00004180 00004025
50000000 0000a000 00004180 0003302e 392e3080 00004180 00004180 00004047
c0000000 0005050e e00ebff0 00000000
```
**可读串**: "version"(0x76657273696f6e) + "0.9.0"(0x302e392e30)
→ cs1 = **应用配置/版本对象**(每次build新建, 非持久cat-state). 含大量 0x41800000(=0x8041 little-endian, Dart null/特定tagged)

### 1.3 ★ cs2 dump(0x90) — 稳定持久, 压缩指针数组
```
011d0036 3c000041 800000eb ff000018 011d0011 44000041 800000eb ff000018
011d00ec 4b000041 800000eb ff000018 011d00c7 53000041 800000eb ff000018
011d0080 60500a25 b0000d1f d00004180 00004180 00004180 00004180 00004180
00004180 00004180 00004180 00004180 00000800 00000000 0002c000 00000800
0000ffff fffe
```
字段(每4字节): @0x7=0x8041(null) @0xb=0xffeb(heap高) @0xf=0x1d0118(ptr) @0x13=0x4411 @0x17=0x8041 ... @0x3f=0x50618 @0x43=0x5ba2 @0x47=0xfdd1
→ cs2像**Map/List结构**(多组 {tag, heap_hi, ptr, value} 条目). 后半大量0x41800000=null填充.
→ cs2可能是**持久配置map**(remoteConfig/dailyState存这里). 需主控解读哪条是isHungry/isDaily.

### 1.4 单例相邻字段
```
single[0x1100,0x30]: 61800000ebff0000 b94fee00ebff0000 41800000ebff0000 41800000ebff0000 b946d700ebff0000 c1820000ebff0000
single[0x25d0,0x30]: 3985ee00ebff0000 c1820000ebff0000 c1820000ebff0000 0a00000000000000 71800000ebff0000 71800000ebff0000
```
注: 0x41800000 / 0x71800000 / 0xc1820000 是 Dart tagged Smi/null 的压缩表示(低字节+heap). c1820000=0x82c1 → 正好是cs2指针低16位(印证cs2=0xffeb000082c1).

---

## 二、0xb9fc84(cat-state函数)未触发分析

- 0xb9fc84 主界面渲染**未HIT**(0xa52920猫build HIT了3次, 但0xb9fc84没)
- 0xb9fc84 加载"饿了喵~"+调isDaily getter, 可能在**设置页每日喂喵**或**阅读页猫点击**时才触发, 非主界面build
- → dump改在0xa52920入口(主界面就触发)拿到的cs1/cs2, 是**猫build时读的state**, 但可能不是0xb9fc84那条isDaily路径

---

## 三、给主控的解读请求 + 下一步

### 请主控解读
1. cs1含version/0.9.0 → 是应用配置(非cat-state?). 那**真正的cat-state(isHungry/isDaily)在cs2的哪个字段?**
2. cs2是Map/List结构 — 哪个entry是isDaily(bool/时间戳)?
3. 或: cat-state不在.field@0x1108/.field@0x25e0, 而在单例别的字段?

### test3下一步(等主控解读, 或并行)
1. **任务C验证**: 主控指出isHungry字段偏移后, runtime写false/改时间戳, 触发rebuild看猫消失
2. **任务B抓guard**: MemoryAccessMonitor监控isHungry字段, 抓读取PC=guard
3. **hook 0xb9fc84触发**: 需进设置页每日喂喵(避开反篡改+广告), 触发0xb9fc84拿它读的state(可能和0xa52920读的不同)

### 待解
- 0xb9fc84主界面不触发, 需操作到每日喂喵/阅读页. 但反篡改spawn抑制下皮皮喵主界面可操作(0xa52920 HIT证明). 需导航到设置页(用户之前建议底部栏第4个齿轮).

---

## 四、资产
- /root/dump_state3.js(0xa52920入口dump cat-state)
- /tmp/cs3.log(完整dump)
- redroid+frida spawn环境就绪, 反篡改spawn抑制有效

> 一句话: dump到cat-state单例(cs1=应用配置含version0.9.0, cs2=持久map结构). 0xb9fc84主界面未触发. 回传主控解读cs2哪条是isHungry/isDaily, 或指引进设置页触发0xb9fc84. 拿到字段→任务C验证+任务B抓guard→根除.
