# 分析报告18 — cs1=解密remoteConfig全量内容+f1028/f0ed0=sentinel(未init)+猫state不在三对象（test3 第十八轮）

> 承报告17+主控"两项修正+cat-state深挖". 完成修正版dump(正确字段f1028/f0ed0/cs1), 结论: 猫state不在这三对象. 回传主控. 中性措辞代码恢复.

---

## 〇、一句话

按主控修正dump了猫build真实读取的field@0x1028/0xed0 + cs1(解密remoteConfig). **f1028=f0ed0=sentinel(0x8071未init), cs1=纯remoteConfig(version/rule/URL/捐赠/蓝奏云)无猫state无时间戳**. 猫isHungry不在这三对象的直接字段. 需主控静态定位isHungry真正存储/计算点.

---

## 一、★ 修正版dump结果(正确字段)

### 1.1 f1028 / f0ed0 = sentinel(未初始化)
```
SINGLE=0xffef1485dd20 (稳定)
f1028 = 0xffeb00008071  ← sentinel(0x8071), 多次HIT一致
f0ed0 = 0xffeb00008071  ← 同一sentinel
```
- f1028(0xa52954读) 和 f0ed0(0xa52a98读) **都指向同一sentinel对象0x8071**
- 这是 lazy-init 的未初始化标记. 猫build读它们时会触发init, 但**onEnter时还没init**
- 注: 0x8071 可能是"默认bool/null"对象. 之前报告说0x1028=isFavChangePos, 但实际猫build读的是sentinel→说明这俩字段在猫build路径里**不是关键state**(只是默认值分支选择)

### 1.2 cs1(field@0x1108) = 解密remoteConfig 全量内容
```
@0x56 "version"  @0x74 "0.9.0"
@0x10a "versionCode"  @0x12c "forceVersionA"  @0x14a "ruleVersion"
@0x16f "forceVersionList"
@0x1a4 "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_7)..." (UA)
@0x1cf "nfoBody?comi" (接口片段)
@0x1fe/0x22e/0x25e "https://vip.lanzoux.com/b932237" (下载URL, 多份)
@0x27a "ruleContent"  @0x299 "ruleBinUrl"
@0x2d2 "https://pages-ghostgzt-...gitlab.io/ppcat/donate.html" (捐赠页)
@0x341 "https://ppcat-share-ghostgzt-...gitlab.io/" (分享页)
@0x3b6 "https://gitlab.com/ghostgzt/ppcat/-/raw/master/version=" (版本检查)
@0x3fe 多镜像config URL(bitbucket/gitlab/gitcode/gitea/gtool.dpdns.org)
```
→ **cs1=服务器下发的解密配置(remoteConfig)**. 含版本/规则/URL/捐赠/分享/下载. **无任何cat/feed/lastFeed/showCat/daily/喵/饿相关key. 无时间戳.**
→ 猫state**不在remoteConfig**.

### 1.3 扫描阴性结论
- 三对象(f1028/f0ed0/cs1) 全量0x400字节 + 跟随指针1层
- **0个时间戳**(epoch ms/µs 2025-2026范围)
- **0个cat相关字符串**(cat/feed/lastFeed/showCat/daily/date/喵/饿)
- f1028/f0ed0 指向sentinel(未init), cs1是remoteConfig

---

## 二、★ 猫isHungry不在已知三对象 → 在哪?

### 已排除
- 单例(thread.field@0x80)前0x400: 全指针+null, 无bool (报告17)
- cs1(field@0x1108)=remoteConfig: 无猫key (本轮)
- cs2(field@0x25e0)=静态map: 改日期不变 (报告16)
- f1028/f0ed0: sentinel未init (本轮)
- 猫build(0xa52920)内部: 不读native时间 (报告17)

### isHungry可能在(需主控判断)
1. **单例的其他field**(非0x1028/0xed0/0x1108/0x25e0). 单例很大(0x25e0+), 还有别的field没dump.
2. **isHungry是重算的**(每次render), 经Dart内部时钟(非libc). 猫build不直接读时间但调用链上层可能读.
3. **猫widget对象(cid0x85e)的@0x30变化Smi**(报告17: 0x0e240204 vs 0x33031a01)可能是isHungry相关, 需解读语义.
4. **0xb9fc84(cat-state函数)读取的field@0x1108/.field@0x25e0深层** — 0xb9fc84主界面不触发(需进设置页每日喂喵), 它读的state可能和猫build读的不同.

---

## 三、给主控的决断请求

静态数据已充分(猫cid0x85e/ctor0xc62438/构造链lr0xc65338/三对象全量dump/调用链全framework). 动态dump isHungry字段连续阴性. 请主控:

1. **静态反编译0xa52920的完整调用链**(从app层build到猫widget构造) — 找app层"if猫饿insert猫"的判断. 主控Ghidra擅长.
2. **或: 指示dump单例其他field**(0x25e0之后的field, 或0x1108/0x25e0之间的field) — isHungry可能在单例未dump的区域.
3. **或: Stalker重追0xa52920执行**(追所有BL, 找时间比较/bool返回的小函数) — 我可做但重.
4. **或: 解读猫widget@0x30 Smi语义**(0x0e240204 vs 0x33031a01) — 可能就是isHungry/猫状态枚举.

### 我的倾向
主控**静态反编译构造链**(选1) — 动态dump连续阴性说明isHungry不是简单字段(是重算或在app层build判断). 主控Ghidra反编译0xc652f8/0xc652b0(app层构造猫的代码)能看"if猫饿"判断, 比动态盲dump高效.

---

## 四、资产
- /tmp/dc.log (cs1全量扫描, 含所有URL/key)
- /tmp/da.log (onLeave dump, 0xa52920未触发此轮)
- /root/{dump_correct.js, dump_after.js}
- redroid+frida spawn就绪

> 一句话: 修正版dump f1028/f0ed0=sentinel(未init)+cs1=纯remoteConfig(version/rule/URL无猫key无时间戳). 猫isHungry不在这三对象. 已排除单例/cs1/cs2/猫build内部. 需主控静态反编译构造链或指示dump单例其他field/Stalker. 倾向主控静态反编译0xc652f8找app层isHungry判断.
