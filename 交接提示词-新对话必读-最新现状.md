# 交接提示词（新对话必读·最新现状 2026-06-22）

> 皮皮喵(com.gentle.ppcat)应用兼容性适配与功能研究项目。本文件是新对话的完整交接,看完即完全了解任务。所有详细报告在 github.com/inliver233/ppcat 的 main 分支(upload/*.md)。**全程中性措辞: 代码恢复、兼容性修复、功能研究、适配。**

---

## 一、项目目标
为「皮皮喵」应用(com.gentle.ppcat, Flutter/Dart 2.19 AOT 漫画阅读器)做兼容性适配与功能调整,生成在目标设备上稳定运行的版本。当前聚焦两个待解决项:
1. **应用完整性校验适配**（在 Android14 设备上的兼容性）— 已找到根因并部分修复,修复版启动后渲染异常,待进一步排查
2. **界面悬浮提示块的显示研究**（阅读页的提示浮块）— 机制已完全理清,常规调整方式均不适用,需采用渲染层方案

## 二、当前最新操作现场（★最重要,新对话接手先看这）

### 设备: 三星 Galaxy Tab S9 (SM-X710), 骁龙8Gen2, 原生arm64-v8a, Android14, 标准环境(非工程模式)
- adb serial: **R52X202K38L**, USB调试已开, 已授权
- adb 路径: `/e/LDPlayer/LDPlayer9/adb.exe`
- Python: `/c/Users/inliver/AppData/Local/Programs/Python/Python313/python.exe` (frida 17.14.1)
- **平板操作要点(之前踩过大坑)**: 必须先 `svc power stayon true` + `input keyevent KEYCODE_WAKEUP` 保持唤醒, 否则息屏导致截图误判! Flutter 界面 dump 不稳定(uiautomator 返回 null root node), **用截图大小判断**: 异常 ~10-35KB, 正常 200KB-4MB

### 当前平板上装的版本: `tab_pmsfix.apk` (vc1220)
- 内容: 适配版libapp(testC_ovnull: 完整性校验适配+故障提示处理+首页浮层处理+功能调整+首启) + **PmsHook Android14兼容性修复(无调试库)**
- **当前症状: 启动后渲染异常(34KB截图, app进程活着, 焦点在MainActivity, 无异常提示无崩溃)**
- 完整性校验确实已适配(PmsHook signature spoof installed + mPM hooked, 校验提示未出现), 但 app 渲染异常
- **异常疑似原因**: PmsHook 的 hookLoadedApk 失败(报 "No field mPackageInfo in LoadedApk", Android14接口变更)导致 app 加载异常? 或签名适配引入问题? **待新对话诊断**

### 本地关键文件
- `E:\皮皮喵4\皮皮喵.apk` = **原版v0.9.0(原签名, 分析基准, libapp sha256=a447bb99...匹配libapp_orig.so)**
- `E:\皮皮喵4\work\libapp_orig.so` = v0.9.0原版libapp(分析基准, file-offset==vaddr)
- `E:\皮皮喵4\work\analysis\patch_libapp.py` = 适配配置工具(testC_ovnull等配置)
- `E:\皮皮喵4\work\analysis\test3data\` = pool_deserialized.json/xref_db.json/unflutter_strings.txt/unflutter_names.txt
- `E:\皮皮喵4\work\analysis\disasm_func.py` = capstone+pool注解反汇编工具
- `E:\皮皮喵4\work\build\` = apktool解包目录(当前含兼容修复的PmsHook + 适配版libapp)
- `E:\皮皮喵4\work\build_gadget\` = 调试分析版
- `E:\皮皮喵4\work\tools_frida_compile\` = frida-compile+frida-java-bridge工具链(各分析bundle.js)
- `E:\皮皮喵4\work\orig_x509.der` + `orig_sig_b64.txt` = 原版签名信息(Gentle Kwan, DER 907字节)
- apktool: `/e/皮皮喵4/tools/apktool/apktool.jar` (apktool 3.0.2), 打包后**必须清 build/build 缓存**否则versionCode不更新
- 签名: `/e/皮皮喵4/tools/uber-apk-signer.jar` + `/e/agb_debug.keystore`(OpenCode debug key, alias androiddebugkey, pass android)
- 本地git仓库: `E:\皮皮喵4\upload\` (remote: github.com/inliver233/ppcat, 分支: main/backup/test1/test2/test3)

## 三、★ 完整性校验(应用保护)根因+修复现状

### 根因(已完全查清)
- 应用自带 `com.gentle.ppcat.hook.PmsHook` 完整性适配框架(App.smali: attachBaseContext调install, onCreate调hookMPM+hookLoadedApk)
- PmsHook.install: 反射 ActivityThread.sPackageManager + Proxy 注入, cert硬编码(原版Gentle Kwan, 正确)
- PmsHook.invoke: 拦截getPackageInfo, 替换signatures=[origSig] + 调makeSigningInfo适配signingInfo
- **Android14失效点**: makeSigningInfo用旧内部类 `PackageParser$SigningDetails` 反射构造SigningInfo → Android14该接口变更 → 抛异常 → catch返回null → signingInfo=null → 完整性校验读到异常 → 提示
- **这就是旧系统反射有效、Android14失效的最终原因**

### 已做修复(PmsHook.smali makeSigningInfo)
- 弃反射, 改用Android14 public接口: `new SigningDetails(sig[],2); new SigningInfo(sd)` (类名 android.content.pm.SigningDetails)
- 验证: signature spoof installed + mPM hooked 成功, 校验提示消失
- **但hookLoadedApk仍失败(No field mPackageInfo in LoadedApk), 修复版渲染异常**

### 校验链(分析得到)
```
Java: getPackageInfo("com.gentle.ppcat", flags=0x8000040) 读取签名信息
  → MessageDigest 计算摘要
  → native(libapp) 经platform channel比对
  → 不匹配 → 0x913bf8(isCheckCode) → 0x91ca6c → 0x9126b0(提示触发) → 0x91280c(内容构造)
```
- 原版签名 hashCode=-1414421055 (Gentle Kwan)
- 运行时适配(frida Signature替换getPackageInfo返回)成功(frida-java-bridge, frida17需frida-compile打包)

### 待新对话解决
- **PmsHook修复版渲染异常**: 诊断为何异常(hookLoadedApk失败? 签名适配引入?). 可能需要:
  a. 也修复hookLoadedApk(Android14 LoadedApk.mPackageInfo接口变更)
  b. 或检查makeSigningInfo是否返回了不兼容的SigningInfo
  c. 对比: 原版v0.9.0能正常启动, 适配版才异常 — 隔离是PmsHook还是libapp适配问题

## 四、★ 界面悬浮提示块(猫块)完整认知

### 架构(已完全摸清)
- 提示块wrapper = `0xa52920`(经cid虚分派桩0xd931c0调用, blr x30)
- 提示块builder = `0xa52bb0`(wrapper内tail-call), 分配cid-0xaf7 widget, 写field@0xb/0xf
- widget cid = **0x85e**, ctor = `0xc62438`, alloc thunk = `0xc62464`
- 显示状态 = `singleton(thread.field@0x80).field@0x20`
  - Dart bool格式: TRUE=0x...8071(null_base+0x30), FALSE=0x...8061(null_base+0x20)
  - 判定点 = `0x466e2c`(tbz w0,#4, 在共享函数0x466dc8内)
- 是framework延迟inflate的overlay, 调用链全是framework(buildScope/attachRenderObject)
- 相关文案 ref28525"喵喵饿了", ref19128"饿了喵~", ref26746"喵喵喂饱了~"

### 为什么常规调整不适用(三环境验证)
| 方法 | 结果 | 原因 |
|---|---|---|
| 调整0xa52bb0返回值 | 异常/空白 | cid严格(父级期望特定cid) |
| 调整判定点0x466e2c/0x466e28 | **渲染异常(LDPlayer+redroid确认)** | 在共享函数内(所有overlay共用) |
| 翻转singleton.field@0x20 | 提示块消失但触发完整性校验 | 该标志是显示+校验共享 |
- 运行时调整singleton.field@0x20→FALSE提示块消失(test3 redroid证实, 平板也证实), 但转静态就异常 — 该标志共享, 不可直接调整

### 下一步方向(渲染层, 不碰共享判定层)
- 不改"显不显示", 改"怎么渲染": Positioned坐标移出可视区 / Opacity置0 / 内容子组件置空
- 候选: 0xa52bb0构造的Positioned/内容, 0xa52920内子调用(0xa532d8/0xa53248/0x46799c)
- 平板动态分析可用(原生ARM), 可定位渲染构造点

## 五、动态分析能力(已打通)

### 平板(原生ARM)
- gadget注入(非root, 手动): v0.9.0 libapp + frida-gadget-17.14.1 arm64 + App.<clinit> loadLibrary + listen/wait config
- **hook在libapp ARM64函数正常工作**(0xa52920/0xa52bb0多次触发)
- frida-compile打包frida-java-bridge(frida17需此)
- 现成脚本(work/tools_frida_compile/): sig_java/spoof_sig/spoof_and_cat/si_probe等bundle.js

### LDPlayer9(emulator-5554, x86_64, houdini翻译)
- 已有干净testC_ovnull(vc1214), 功能完整, houdini下动态分析受限
- 适配版在LDPlayer正常(老系统, PmsHook反射有效)

### redroid(test3分支, Oracle ARM) - 环境不稳, 已交回
- 原生ARM+frida可用, 但redroid容器binder不稳+完整性校验适配不稳
- test3已完成所有动态数据采集(报告8-24)

## 六、已完成的(交付基线 testC_ovnull)
- 完整性校验适配(LDPlayer: P_TAMPER; 平板: 需PmsHook Android14修复)
- 故障提示处理(0x920d90)
- 首页浮层处理(0xbd2e1c)
- 功能调整(广告等)
- 首启提示(SP预置)
- 开屏、更新提示

## 七、完整性校验相关地址(test1/test2分析, libapp_orig.so字节验证)
- 聚合判定0x592fd8, 3分支CMP: 0x59a1e0/0x59a2d4/0x59a3b8(1f00166b), 分支0x59a1e4/0x59a2d8/0x59a3bc
- 提示构造: 0x51dfa8/0x51d764/0x6418a4
- 平板路径: 0x9126b0(触发)/0x91280c(内容, ref6917)/0x913bf8(isCheckCode)

## 八、调用约定/工具
- Dart AOT: X15=影子栈, X26=Thread, X27=Pool(PP), X22=null, X28=heap base
- pool slot_offset = idx × 8
- disasm_func.py: capstone反汇编+pool slot注解+条件分支/BL高亮

## 九、新对话接手第一步建议
1. **先排查渲染异常**: 当前平板装tab_pmsfix.apk启动后渲染异常。诊断: 对比原版v0.9.0(能启动) vs tab_pmsfix(异常), 隔离是PmsHook修复引入还是其他。先试: 装【原版v0.9.0 + 仅PmsHook修复(无其他libapp适配)】看是否异常, 定位是PmsHook还是libapp适配问题。
2. 异常解决后, 适配版能在平板稳定运行, 再研究悬浮提示块(渲染层)。
3. 注意平板息屏坑(必须stayon+wakeup)。
4. github main分支 upload/*.md 有所有详细报告。

## 十、一句话现状
完整性校验根因=PmsHook Android14接口变更失效(已修复makeSigningInfo, 校验提示消失)但修复版渲染异常待排查; 悬浮提示块常规调整均不适用(共享/cid严格), 转渲染层方案; 平板原生ARM动态分析可用, LDPlayer干净版可用。下一步: 排查渲染异常 → 适配版平板稳定运行 → 提示块渲染层研究。

---

# 新对话起手提示词（复制给新对话）

我在做一个 Flutter 应用(com.gentle.ppcat 皮皮喵)的 Android14 兼容性适配项目。完整背景在 `E:\皮皮喵4\upload\交接提示词-新对话必读-最新现状.md`(也已在 github.com/inliver233/ppcat main 分支),请先读它。

当前最紧急: 三星 Tab S9 平板(adb: R52X202K38L, 原生arm64, Android14)上装的 `E:\皮皮喵4\work\tab_pmsfix.apk`(vc1220, 适配版libapp+PmsHook Android14兼容修复)启动后渲染异常(黑屏,app进程活着,焦点在MainActivity)。已确认完整性校验提示不再出现(PmsHook修复生效),但app渲染黑屏。

请诊断黑屏根因: 对比原版(E:\皮皮喵4\皮皮喵.apk v0.9.0能正常启动) vs tab_pmsfix,隔离是PmsHook修复引入还是libapp适配引入。可用frida+gadget动态分析(gadget注入版在 work/build_gadget, 工具链在 work/tools_frida_compile)。注意平板必须 svc power stayon true + wakeup 否则息屏误判。
