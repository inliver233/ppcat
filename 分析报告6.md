# 第六轮代码恢复分析报告

> 分支：`test1`
>
> 角色：纯静态分析，只读仓库文件，产出代码恢复报告、脚本与 patch 清单；不修改原始 APK / so / dex。

## 1. 本轮结论

本轮 `test1` 的有效新增，不是“所有问题都已闭环”，而是把三件事收口到了可直接执行的证据层级：

1. **Java 广告桥接的关键纠偏已独立证实**：`flutter_pangle_ads` 不是单一 `MethodChannel` 插件，而是 `MethodChannel + EventChannel` 双通道；其真正 handler 类是 [C0984.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p058/C0984.java:1)，同时实现 `C0932.InterfaceC0935` 与 `C0945.InterfaceC0949`。因此，前几轮那种“统一 fake 一个 MethodChannel no-op handler 就够了”的说法，在 `Pangle` 上不成立。`GDT` 本地混淆实现也不是 EventChannel 形态，而是**每次 reward 请求动态创建子 `MethodChannel`**。
2. **第三故障来源有了高置信候选**：基于 `pool_accesses.txt`，`ref=27673` 对应的故障正文 slot `0x013c68` 除 `0xbbc22c` builder 外，还被 `0x913bf8` 访问；其内部两个 dialog-show `BL` 位点 `0x914e74 / 0x914f24` 与已被运行时验证安全的 `0xbc0f40/0xbd5a24` 模式一致，适合作为第三故障来源候选 patch。
3. **对象池在 test1 侧仍未彻底闭环**：`slot -> PC` 交叉验证是成立的；但“完整 ref->slot 反序列化”在 `test1` 本轮仍不能诚实宣称已完成。官方 Dart 2.19 源码确认对象池 fill entry 不是旧报告假设的 `ReadUnsigned(type)` 形式，而是每 entry 一个原始 `uint8 entry_bits` 后跟 type-dependent payload；`unflutter` 在 ppcat 定制 VM 的 pool parse 仍卡在 `type=103 / 0x67`。`test2` 所用的 `eb & 3` 启发式，本轮只能当作候选规律，不能直接升级为 `test1` 已证实规则。

因此，本轮交付策略是：

- 对 **Java no-op / reward 假成功**，只交付“已证实结构 + 可执行最小替代方案 + 未闭环卡点”。
- 对 **第三故障来源**，交付“高置信候选 patch”，明确它比前几轮更接近运行时可测，但仍保留“尚待主控实机二次验证”的标注。
- 对 **对象池**，如实报告 `test1` 当前能独立复核到哪一步，避免把 `test2` 的结论直接冒充为本分支已独立突破。

## 2. 输入与交叉验证

本轮实际阅读/复核：

- `origin/main:任务文档6.md`
- `origin/backup:backup/第七轮弹窗攻关/关键教训.md`
- `origin/backup:backup/第七轮弹窗攻关/第七轮交接.md`
- `origin/test2:分析报告6.md`
- 本分支已有：
  - `unflutter_dump/asm.txt.gz`
  - `unflutter_dump/snapshot.json`
  - `unflutter_strings.txt`
  - [analysis_round4/plugin_binding_plaintexts.tsv](/root/ppcat_repo/analysis_round4/plugin_binding_plaintexts.tsv)
  - [analysis_round4/plugin_binding_methods.txt](/root/ppcat_repo/analysis_round4/plugin_binding_methods.txt)
- 本轮新增本地产物：
  - [analysis_round6/decode_reward_events.py](/root/ppcat_repo/analysis_round6/decode_reward_events.py)
  - [analysis_round6/reward_events_decoded.txt](/root/ppcat_repo/analysis_round6/reward_events_decoded.txt)
  - [analysis_round6/scan_snapshot_pool_refs.py](/root/ppcat_repo/analysis_round6/scan_snapshot_pool_refs.py)
  - `analysis_round6/pool_accesses.txt`（本地分析输入，未纳入提交）

与 `test2` 的交叉验证结论：

- **一致**：
  - `Pangle` 存在 `flutter_pangle_ads` / `flutter_pangle_ads_event` 双通道。
  - `GDT` reward 路径与 `Pangle` 不同：本地混淆类 [C1083.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p073/C1083.java:1) 会通过 `new C0932(..., m3249(str))` 为每次 reward 请求创建子 `MethodChannel`，其明文前缀可解为 `plugins.hetian.me/gdt_plugins/RewardVideo/`。
  - `pool_accesses.txt` 中 `0x913d3c -> 0x013c68`、`0x89b000 -> 0x02cf40`、`0xb5af3c -> 0x04cb98` 等 `slot -> PC` 命中成立。
  - `0x913bf8` 是强候选第三故障来源。
- **分歧 / test1 保守处理**：
  - `test2` 报告把“对象池 entry 结构突破”和“21707/22466/29112 精确 slot 已完成”写得较强；`test1` 本轮不直接升级为独立事实，因为我没有在 `test1` 侧把完整 pool parser 跑通并通过 4-anchor 自洽验证。
  - `test2` 报告把 `entry_bits -> 类型` 概括为 `eb & 3`；`test1` 本轮不采纳为已证实规则。官方 Dart 2.19 的 `TypeBits` 定义在 [object_pool_builder.h](/root/dartsdk/runtime/vm/compiler/assembler/object_pool_builder.h:28) 明确是 `BitField<uint8_t, EntryType, 0, 7>`，即低 7 位编码类型，不能直接等同于低 2 位分类。
  - `test2` 报告里给出的通用 Java stub，`test1` 认为对 `Pangle` 仍证据不足，不应视为“已完成可直接信任”。

## 3. 运行时硬约束复核

以 `backup/第七轮弹窗攻关/关键教训.md` 为准，本轮继续遵守：

- `0xbc0f40` / `0xbd5a24` / `0x7e1464` **入口**禁止改 `MOV X0, X22; RET`
- `0x7e1464` **内部 dialog-show BL** 禁止 `NOP`
- 已验证安全 patch 仍为：
  - `0xbc1020: a0 12 00 54 -> 00 02 00 54`
  - `0xbc1058/0xbc1134/0xbc1234 -> 1f 20 03 d5`
  - `0xbd60ac/0xbd6130/0xbd6264/0xbd6300 -> 1f 20 03 d5`

这一点非常重要：本轮所有新建议都只沿用“函数内 dialog-show BL 改 NOP”这条已被运行时事实证明安全的手法，不再回退到“整函数入口 return null”的旧建议。

## 4. Java 侧广告桥接：本轮已独立证实的结构

### 4.1 Flutter 混淆 API 映射

`classes2.dex` 中已独立复核的混淆 API：

| 混淆类 | 实际角色 | 证据 |
|---|---|---|
| `p052.C0932` | `MethodChannel` | [C0932.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p052/C0932.java:1) |
| `p052.C0932.InterfaceC0935` | `MethodCallHandler` | [C0932.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p052/C0932.java:70) |
| `p052.C0932.InterfaceC0936` | `Result` | [C0932.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p052/C0932.java:76) |
| `p052.C0945` | `EventChannel` | [C0945.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p052/C0945.java:1) |
| `p052.C0945.InterfaceC0949` | `StreamHandler` | [C0945.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p052/C0945.java:147) |
| `p052.C0945.InterfaceC0950` | `EventSink` | [C0945.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p052/C0945.java:155) |
| `p094.InterfaceC1287` | 插件接口 / binding 容器 | [InterfaceC1287.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p094/InterfaceC1287.java:1) |

关键行为：

- `MethodChannel.setMethodCallHandler(...)` 对应 `C0932.m2780(...)`
- `EventChannel.setStreamHandler(...)` 对应 `C0945.m2793(...)`
- `Result.success(...)` 对应 `InterfaceC0936.mo1055(Object)`
- `EventSink.success(...)` 对应 `InterfaceC0950.mo2008(Object)`
- `EventSink.endOfStream` / cancel 路径由 `mo2009()` 及 `mo2030()` 负责

### 4.2 Pangle 不是普通单通道插件

`Pangle` 主插件类是 [C0981.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p058/C0981.java:1)。

独立解出的两个 channel 名：

- `flutter_pangle_ads`
- `flutter_pangle_ads_event`

证据：

- `C0981.mo1050(...)` 中：
  - `this.f2450 = new C0932(..., "flutter_pangle_ads")`
  - `this.f2451 = new C0945(..., "flutter_pangle_ads_event")`
- `C0981.mo2146(...)` 中：
  - `this.f2450.m2780(c0984)` 把同一个 `C0984` 挂到 `MethodChannel`
  - `this.f2451.m2793(this.f2452)` / `this.f2451.m2793(null)` 走 `EventChannel`

`C0984` 本身同时实现：

- `C0932.InterfaceC0935`：消费 `flutter_pangle_ads` 的 method call
- `C0945.InterfaceC0949`：处理 `flutter_pangle_ads_event` 的 listen/cancel，并保存 `EventSink`

对应源码：

- [C0984.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p058/C0984.java:1)
- `mo2029(Object, EventSink)`：保存 `this.f2466 = interfaceC0950`，即保存 event sink
- `mo2030(Object)`：清理 `this.f2466 = null`
- `mo28(MethodCall, Result)`：method 分发入口

这说明：

- **Pangle 的 reward 成功，不是单纯 `result.success(null)` 就完事**
- 还必须有 **EventChannel 侧的事件发送**，否则 Dart 端 reward 流程大概率仍会停在“监听不到成功事件”

这也是本轮最重要的纠偏。

### 4.3 Pangle method -> helper 的静态落点

通过对 [C0984.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p058/C0984.java:1170) 的解码与 helper 调用复核，可确认至少这四条：

| method 名 | helper / 行为 |
|---|---|
| `initAd` | `TTAdSdk.init(...)`，见 [C0984.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p058/C0984.java:482) |
| `showFullScreenVideoAd` | `new C1297().m3695(...)`，见 [C0984.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p058/C0984.java:1255) |
| `showRewardVideoAd` | `new C1292().m3695(...)`，见 [C0984.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p058/C0984.java:1357) |
| `showSplashAd` | `new C1299().m3695(...)`，见 [C0984.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p058/C0984.java:1455) |

同时，`requestPermissionIfNecessary` 与 `setUserExtData` 也能在 `C0984` 中看到对应逻辑。

### 4.4 reward / event 的已解码明文

本轮新增脚本：

- [decode_reward_events.py](/root/ppcat_repo/analysis_round6/decode_reward_events.py)

产物：

- [reward_events_decoded.txt](/root/ppcat_repo/analysis_round6/reward_events_decoded.txt)

已独立解出的关键明文：

- `Pangle` event 名：
  - `onAdClose`
  - `onAdClosed`
  - `onAdShow`
  - `onAdLoaded`
  - `onRewardVideoAdLoad`
  - `onRewardVideoCached`
  - `onVideoComplete`
  - `onAdReward`
  - `onAdError`
- `Pangle` reward map key：
  - `action`
  - `adId`
  - `errCode`
  - `errMsg`
  - `customData`
  - `rewardType`
  - `rewardName`
  - `rewardVerify`
  - `rewardAmount`
  - `userId`
- `GDT` event：
  - `onReward`
  - `onADClose`
  - `onADShow`
  - `onADLoad`
  - `onVideoComplete`
- `Kwad` event：
  - `onReady`
  - `onClose`
  - `onVerify`
  - `onShow`
  - `hasReward`
  - `rewardAmount`
  - `rewardName`

这些字符串说明：reward 成功的“事件级回调协议”是真实存在的，而且 `Pangle` / `GDT` / `Kwad` 三家格式并不完全相同。

### 4.5 结合上游插件源码后的结构结论

本轮另外交叉阅读了上游开源插件：

- `external_src/flutter_pangle_ads/android/src/main/java/com/zero/flutter_pangle_ads/page/RewardVideoPage.java`
- `external_src/flutter_qq_ads/android/src/main/java/com/zero/flutter_qq_ads/page/RewardVideoPage.java`

与本地混淆实现对上后，可独立确认两点：

1. **Pangle reward 成功链条依赖 EventChannel**
   - 上游 `RewardVideoPage` 的顺序是：`onRewardVideoAdLoad -> onAdLoaded`，`onRewardVideoCached(TTRewardVideoAd) -> showRewardVideoAd + onAdPresent`，`onAdShow -> onAdExposure`，`onRewardArrived -> onAdReward`，`onAdClose -> onAdClosed`。
   - 本地 [C0984.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p058/C0984.java:1) 的 `mo2029/mo2030` 明确维护 `EventSink f2466`，说明 reward 成功不能只靠 `MethodChannel.Result.success(...)`。
2. **GDT reward 成功链条走每次请求独立子 channel**
   - 上游 `flutter_qq_ads` 的 `RewardVideoPage` 通过 `onADLoad -> onAdLoaded`、`onADShow -> onAdPresent`、`onADExpose -> onAdExposure`、`onReward -> onAdReward`、`onADClose -> onAdClosed` 发事件。
   - 本地 [C1083.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p073/C1083.java:65) 构造时调用 `new C0932(C1103.f2851.m3684(), m3249(str))`，而解密产物已给出前缀 `plugins.hetian.me/gdt_plugins/RewardVideo/`。这说明本地 GDT reward 更接近“动态子 MethodChannel 回调”，而不是 `Pangle` 那种固定 EventChannel。

## 5. Java no-op / reward 假成功：test1 的实际建议

### 5.1 本轮不再背书的说法

`test1` 本轮**不直接背书**“通用新建一个 fake 插件类 + 统一 MethodChannel success(null) + 人工 invoke 几个事件”已经足够，原因：

1. `Pangle` 已证实需要 **EventChannel 级别** 的 listen/cancel + event sink。
2. `C0984` 同时承担 method 分发、event sink 保存、以及多个 helper 的路由逻辑；不是简单单方法 stub 能完全替代。
3. 虽然已解出 reward 事件名和 map key，但 `Dart` 端对事件的时序与必填字段要求，纯静态下还没有完整闭环。

因此，`test2` 报告里“完整 Java stub 已完成”的说法，在 `test1` 看来证据仍偏强。至少 `Pangle` 和 `GDT` 就不是同一种回调拓扑，不能共用一个过度抽象的 fake handler。

### 5.2 当前最稳妥的最小替代方案

如果主控要优先做“最小改动、尽量不炸”的 Java 侧代码恢复，我建议顺序如下：

1. **先恢复插件注册，消灭 MissingPluginException**
2. 对明确实现 `MethodCallHandler` 的插件，优先短路返回 `success(Boolean.TRUE)` 或 `success(null)`
3. `Pangle` 单独处理，优先复用 `C0981/C0984` 原结构，而不是新造一个总 stub
4. `GDT` 也不建议套 `Pangle` 模板；更稳妥的是保留原 `C1103/C1083` 结构，只在 reward 关键分支里短路成功回调

原因：

- `GDT`、`Kwad`、`AdMob` 的 `MethodChannel` 结构相对直白
- `Pangle` 的 method/event 双通道必须分开恢复

### 5.3 对 Pangle 的更小风险路线

在 `Pangle` 上，比“另起炉灶造 fake 插件”更稳妥的路线是：

- 继续使用原 `C0981 -> C0984` 结构注册 `flutter_pangle_ads` / `flutter_pangle_ads_event`
- 在 `C0984` 的 method 分发中，**只短路最关键的广告方法**
  - `initAd`
  - `showRewardVideoAd`
  - `showFullScreenVideoAd`
  - `showSplashAd`
- 对 `showRewardVideoAd`，若要做“喂喵成功”路径，必须仿照 `C1292` 的真实 event 协议，向 `this.f2466`（EventSink）推送 reward success + close 事件

但这里我仍然要如实说明：

- **test1 本轮没有把这一套 smali 直接写成可装机版本**
- 因为仅靠纯静态，还不足以证明最小字段集合与事件时序一定正确

也就是说，本轮在任务 D 上的价值是：

- 明确指出了此前方案的结构性缺口
- 给出了更可信的改造方向
- 但没有虚构“已完成可直接过机”的 stub

## 6. 对象池：test1 本轮的真实状态

### 6.1 已独立确认

官方 Dart 2.19 源码（`/root/dartsdk/runtime/vm/app_snapshot.cc` 与 `object_pool_builder.h`）表明：

- object pool fill 是“每 entry 一个原始 `uint8 entry_bits` + type-dependent payload”
- `ObjectPoolSerializationCluster::ReadFill` 在 [app_snapshot.cc](/root/dartsdk/runtime/vm/app_snapshot.cc:2585) 直接 `Read<uint8_t>()` 读取 `entry_bits`
- `TypeBits` 定义在 [object_pool_builder.h](/root/dartsdk/runtime/vm/compiler/assembler/object_pool_builder.h:49)，类型位宽是 7 bit，不是 2 bit
- 不是旧 `test1` 报告误写的“type 本身走 `ReadUnsigned()`”

因此，旧假设需要废弃。

### 6.2 七个关键 ref 的独立 anchor 校验

本轮新增脚本 [scan_snapshot_pool_refs.py](/root/ppcat_repo/analysis_round6/scan_snapshot_pool_refs.py:1) 可直接在 `libapp.so` 上扫描 `0x80 + VLE(ref)`，输出 isolate snapshot 中的相对偏移与文件偏移。其作用不是解出 slot，而是把七个关键 anchor 变成可重复运行的本地证据。

脚本默认验证的 7 个 ref 及其期望文件偏移：

| ref | 文件偏移 |
|---|---|
| `26842` | `0x2eb53c` |
| `30947` | `0x2eb540` |
| `29112` | `0x2ece05` |
| `27673` | `0x2eeeca` |
| `30922` | `0x2f0415` |
| `21707` | `0x2f8c81` |
| `22466` | `0x305c5a` |

这 7 个 anchor，本轮在 `unflutter/internal/cluster` 的 `TestScanPpcatPoolTaggedWindows` 和 `TestInspectPpcatRefNeighborhoods` 上也再次独立跑通，和上表完全一致。

### 6.3 `slot -> PC` 交叉验证成立

从 `analysis_round6/pool_accesses.txt` 独立复核到的关键命中：

| PC | slot offset | 含义 |
|---|---|---|
| `0x00913d3c` | `0x013c68` | 故障 body slot |
| `0x00bbc31c` | `0x013c68` | 故障 body builder 已知点 |
| `0x006349e8` | `0x00e8a8` | `test2` 认为是故障 title slot，test1 认可 slot->PC 但不单独宣称 ref 已闭环 |
| `0x0089b000` | `0x02cf40` | 每日喂喵 title 唯一加载点 |
| `0x00b5af3c` | `0x04cb98` | 每日喂喵 body 唯一加载点 |

其中：

- `0x02cf40` 只有 `0x89b000` 这一处加载
- `0x04cb98` 只有 `0xb5af3c` 这一处加载

这使得“每日喂喵标题/正文 builder”在 `slot -> PC` 层面已经很清楚。

### 6.4 `test1` 仍未独立完成的部分

`unflutter` 在 ppcat 定制 VM 的对象池 parse 仍卡在：

- pool stream 起点附近 `0x2e0bfb`
- unknown type `103 / 0x67`

因此本轮 `test1` 不宣称：

- “完整 ref->slot 反序列化已完成”
- “21707/22466/29112 的精确 slot 已由 test1 parser 独立求出”

更准确的说法是：

- `slot -> PC` 交叉验证成立
- `ref -> slot` 仍部分依赖 cross-branch 结果
- `test2` 的 `eb & 3` 解析法目前只可作线索，不可当作 `test1` 已证实的 pool 规范

## 7. 第三故障来源：`0x913bf8` 高置信候选

### 7.1 命中故障正文 slot

`pool_accesses.txt`：

```text
0x00913d3c  0x013c68  0x00913d3c  10 36 46 f9  LDR X16, [X16,#3176]
0x00bbc31c  0x013c68  0x00bbc31c  10 36 46 f9  LDR X16, [X16,#3176]
```

已知 `0x013c68` 是故障正文 `ref=27673` 的 slot。

也就是说，`0x913bf8` 与已知故障 body builder `0xbbc22c` 确实落在同一故障正文对象上。

### 7.2 函数形态

函数入口：

```asm
0x00913bf8  fd 79 bf a9  STP X29, X30, [X15,#-16]!
0x00913bfc  fd 03 0f aa  MOV X29, X15
0x00913c00  ef 81 0e d1  SUB X15, X15, #0x3a0
```

这是典型 async state machine，形态与已知广告/故障调度函数同类。

### 7.3 两个 dialog-show BL 位点

#### 位点 1：`0x914e74`

```asm
0x914e6c  STP X2, X1, [X15,#-16]!
0x914e70  STP X4, X3, [X15,#-16]!
0x914e74  0a 7a ed 97  BL .+0xffffffffffb5e828   ; target = 0x47369c
0x914e78  ADD X15, X15, #0x20
0x914e7c  MOV X0, X22
```

#### 位点 2：`0x914f24`

```asm
0x914f1c  STP X1, X3, [X15,#-16]!
0x914f20  STP X4, X0, [X15,#-16]!
0x914f24  5d 77 ed 97  BL .+0xffffffffffb5dd74   ; target = 0x472c98
0x914f28  ADD X15, X15, #0x20
0x914f2c  MOV X0, X22
```

这两个位点与主控已经验证安全的 `0xbc0f40/0xbd5a24` 模式完全一致：

- `BL` 前有 2 个 `STP` push
- `BL` 后有配对 `ADD X15, #0x20`
- 返回值马上被 `MOV X0, X22` 覆盖

### 7.4 patch 建议

**高置信候选 patch：**

| 地址 | 原始字节 | patch 字节 | 说明 |
|---|---|---|---|
| `0x914e74` | `0a 7a ed 97` | `1f 20 03 d5` | `BL 0x47369c -> NOP` |
| `0x914f24` | `5d 77 ed 97` | `1f 20 03 d5` | `BL 0x472c98 -> NOP` |

> 注意：`0x913bf8` **入口** 不可改 `MOV X0, X22; RET`，仍遵守第七轮运行时硬约束。

### 7.5 本轮立场

`test1` 对 `0x913bf8` 的表述是：

- **高置信候选第三故障编排器**
- 证据强于前几轮
- 但尚未在 `test1` 本轮自行补出“同链故障 title slot 也命中该 caller”的完整闭环

所以我建议主控把它作为下一优先级 patch-and-test 点，而不是在报告里写成“100% 已独立最终确认”。

## 8. 每日喂喵：当前可独立落到的层级

从 `pool_accesses.txt` 可独立复核：

```text
0x0089b000  0x02cf40  ...  LDR X4, [X4,#3904]
0x00b5af3c  0x04cb98  ...  LDR X3, [X3,#2968]
```

对应含义：

- `0x89b000`：每日喂喵标题的唯一 `.text` 加载点
- `0xb5af3c`：每日喂喵正文的唯一 `.text` 加载点

这与 `test2` 的 builder 归属是一致的，但 `test1` 本轮没有进一步独立证明“上层 showDialog 编排器入口”，所以不在本轮冒进给出每日喂喵的 libapp patch。

结论：

- **从 Dart 侧继续追每日喂喵，仍依赖对象池进一步闭环**
- **从 Java 侧消灭 reward 失败** 仍是更高杠杆路线

## 9. VIP / 更新

这一轮 `test1` 的新增，不是“已经拿到单点 isVip patch”，而是把 `VIP 内容页`、`共享状态门控`、`过期字段读取`、`更新弹窗` 四条线拆开了。

### 9.1 `0x911788`：共享状态门控器，而不是 VIP 专用函数

对 `libapp.so` 的直接反汇编显示，`0x911788` 不是单纯 `isVip()`，而是一个被多路复用的共享状态读取器。它目前至少被这些函数直接 `BL` 调用：

- `0x8a0fd4`
- `0x920d7c`
- `0xb9fc84`
- `0xbca240`
- 以及 `0x590370 / 0x7e3d80 / 0x7e85dc / 0x7eba74 / 0x7f7898 / 0x80a384 / 0x8391dc / 0xa581c0 / 0xa92e04 / 0xaf20b0 / 0xb04bd8 / 0xba2848 / 0xbe4954 / 0xc46bb0`

函数内部的关键证据：

- `0x91185c` 加载 `pool[0x35a8] -> ref=26968 "remoteConfigSign"`
- `0x91191c` 加载 `pool[0x19da0] -> ref=18250 "Main"`
- `0x91196c` 加载 `pool[0x100] -> ref=1579 "Invalid internet address "`
- `0x911aa4` 读取 `[x26,#0x80]+0xea8` 的时间字段，再和当前值做秒级差值比较
- `0x911ae4` 最终通过 `CSEL` 在 `x22+0x20` / `x22+0x30` 两个 canonical bool 之间返回

更重要的是：调用方几乎都用 `tbnz/tbz w0,#4` 检查它的返回值。由于 `x22+0x30` 的 bit4 置位、`x22+0x20` 的 bit4 不置位，可以独立推出：

- `0x911788` 返回 `x22+0x30` = “通过 / 继续”
- `0x911788` 返回 `x22+0x20` = “不通过 / 提前退出”

结合 `0x911ad0..0x911aec` 的秒级差值比较，当前最稳的结论是：

- `0x911788` 是一个 **带时间阈值的共享门控器**
- 它会综合 `remoteConfigSign / Main / Invalid internet address / 上次记录时间` 等状态
- 最终决定“某条需要网络/广告/配置的路径是否允许继续”

这能解释为什么它会同时出现在：

- `0x920d7c`（故障正文构造链）
- `0x8a0fd4`（“可以捐赠”提示）
- `0xb9fc84`（VIP 页面上层状态机）
- `0xbca240`（另一条带计数/阈值的状态链）

因此，后续如果要继续攻 VIP，不该再把 `0x911788` 简化理解成“一个布尔 isVip”。

### 9.2 `0xb9fc84`：VIP 页面上层混合状态机，不宜硬猜单分支 patch

`0xb9fc84` 的前半段先调用 `0x911788`，随后进入大量 UI 构造与状态分流。关键字符串落点已经能独立复核：

- `0xb9fe54` -> `ref=26299` -> `未获取`
- `0xb9fe94` -> `ref=3877` -> `未登录`
- `0xba0070` -> `ref=13169` -> `功能，通过捐赠绑定账号获取`
- `0xba007c` -> `ref=8910` -> `通过捐赠绑定设备获取`
- `0xba027c` -> `ref=4505` -> `点击头像绑定账号`
- `0xba02e0` -> `ref=24111` -> `解绑`
- `0xb9fdb8` -> `ref=17423` -> `isDaily:`
- `0xba07fc` -> `ref=2878` -> `24小时内累计浏览`
- `0xba07d0` -> `ref=13199` -> `次推荐信息，即可获取`
- `0xba09e8` -> `ref=8726` -> `喵喵喵~`
- `0xba0bcc` -> `ref=19128` -> `饿了喵~`

这段代码的实际角色更像：

1. 先经过 `0x911788` 判断当前全局状态是否允许继续。
2. 再根据“未登录 / 未获取 / 账号绑定 / 设备绑定 / 每日状态 / reward 浏览次数”等条件，构造不同的页面区块或提示对象。
3. 非 VIP 路径会落到 `0xba0070 / 0xba007c` 这两条“需要捐赠绑定”的文案分支。

所以当前更稳的表述是：

- `0xb9fc84` 是 **VIP 页面上层混合状态机**
- 它同时混入了 **每日喂喵 / reward 次数 / 喵喵状态** 这条线
- 它确实包含“非 VIP 提示路径”
- 但它不是一个可以直接负责任改成“恒 true”的单比较点

本轮仍不建议对 `0xb9fc84` 粗暴做入口 return-null，也不建议凭猜测改某个早期 `b.eq/b.ne`。

### 9.3 `0xa54178 / 0x8a0fd4 / 0x8ba3d0 / 0x67994c`：四条线已经能分清

基于对象池 + 本地反汇编，VIP 相关现在可以稳定拆成四类函数：

- `0xa54178`：**VIP 内容页主构造区**
  - `0xa54d3c` -> `8910` `通过捐赠绑定设备获取`
  - `0xa54e54` -> `9292` `已获取`
  - `0xa54e80` -> `29757` `未获得`
  - `0xa55130` -> `13169` `功能，通过捐赠绑定账号获取`
  - `0xa555ac` -> `2878` `24小时内累计浏览`
  - `0xa555e4` -> `13199` `次推荐信息，即可获取`
  - `0xa5571c` -> `13928` `，未获得特权将在0点重置浏览次数！`
  - `0xa55858` -> `3524` `已生效`
  - `0xa579c0` -> `10073` `已绑定账号`
  - `0xa55c80` -> `21707` `每日喂喵`
  - `0xa55cbc` -> `28525` `喵喵饿了`
  - `0xa55f30` -> `21925` `训练喵喵`
  - `0xa55f8c` -> `31475` `让喵喵更智能`
  - `0xa56200` -> `17788` `每日一次`
- `0x8a0fd4`：**“可以捐赠”提示构造器**
  - 入口立刻 `BL 0x911788`
  - `0x8a10d0` -> `11366` `可以捐赠`
  - 这条链还检查 `[x26,#0x80]+0xb40 / +0x2540 / +0xee0 / +0x1108`，明显不是纯文案函数
- `0x8ba3d0`：**`expiresDate` 读取链**
  - `0x8ba8e0` -> `7406` `expiresDate`
  - 该函数会把 key 送入后续查找/比较逻辑，不是简单字符串展示
- `0x67994c`：**`expires` 判定 / 格式化链**
  - `0x6799dc` -> `15609` `date`
  - `0x6799e4` -> `12056` `expires`
  - 之后会返回 `x22+0x20 / x22+0x30`，再进入后续字符串/对象构造

因此，当前最稳的 VIP 结论不是“已找到 isVip patch”，而是：

- `0xa54178` 负责 **VIP 内容页 + 每日喂喵/训练喵喵 UI 片段**
- `0xb9fc84` 负责 **VIP / 登录绑定 / 每日 reward 状态编排**
- `0x8a0fd4` 负责 **可捐赠提示**
- `0x8ba3d0 / 0x67994c` 负责 **过期字段读取 / 过期判定**

### 9.4 更新窗：维持 caller 侧 `BL -> NOP` 方案

更新链的结论没有变化，但现在能更明确地说明其职责拆分：

- `0x85efd4`：构造 `发现新版本 / 新版本 ...` 相关对象
  - `0x85f020` -> `5520` `发现新版本`
  - `0x85f060` -> `19264` `新版本 `
- `0x85f09c`：后续更新内容拼装
- `0xdac684`：更新 URL 选择

从安全性看，仍然优先推荐 caller 侧两个 dialog-show `BL`：

- `0x8667a8`：原始字节 `bd33f097` -> patch `1f2003d5`
- `0x8675bc`：原始字节 `3830f097` -> patch `1f2003d5`

原因不变：

- 两处都符合“2 个 `STP` push -> `BL` -> `ADD X15,#0x20` -> `MOV X0,X22` -> `RET`”的安全模式
- 比直接改 `0x85efd4` 入口更保守

### 9.5 新突破：`shared_preferences` 桥接已经能落到 Dart AOT 访问点

这一轮新增工具后，`test1` 终于把“Flutter 本地持久化”这条线从猜测拉到了证据层。

先看 Java/dex 侧：

- [C0661.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p026/C0661.java:1) 注册的 channel 是 `plugins.flutter.io/shared_preferences`
- [C0659.java](/root/ppcat_repo/analysis_round6/out/jadx_classes2/sources/p026/C0659.java:1) 是它的 `MethodCallHandler`
- `C0659` 明确使用：
  - `context.getSharedPreferences("FlutterSharedPreferences", 4)`
  - 仅处理 `flutter.*` 前缀键
  - 并实现 `getAll / setBool / setDouble / setInt / setString / setStringList / remove / clear`

再看 Dart AOT / 对象池侧，这些 method 名字符串都已能精确落到 slot 和 `.text` 访问点：

- `getAll`
  - `ref=26533`
  - `slot=0xac1 / off=0x5608`
  - `.text` 访问：`0x4ea820 / 0x641248 / 0x8ca9d0 / 0x8cb82c / 0x8cc1e8 / 0x8ccdb4 / 0x903cf8`
- `setBool`
  - `ref=10441`
  - `slot=0x29a8 / off=0x14d40`
  - `.text` 访问：`0x599648`
- `setDouble`
  - `ref=21439`
  - `slot=0x29ad / off=0x14d68`
  - `.text` 访问：`0x599800`
- `setInt`
  - `ref=9324`
  - `slot=0x29b2 / off=0x14d90`
  - `.text` 访问：`0x599a04`
- `setString`
  - `ref=20644`
  - `slot=0x29b7 / off=0x14db8`
  - `.text` 访问：`0x599c04`
- `setStringList`
  - `ref=14511`
  - `slot=0x29bc / off=0x14de0`
  - `.text` 访问：`0x599e08`
- `clear`
  - `ref=8772`
  - `slot=0x29c3 / off=0x14e18`
  - `.text` 访问：`0x59a1cc`

这说明：

- Flutter `shared_preferences` 插件在本包里是 **真实接通的**
- Dart AOT 里也能看到完整 method-name 桥接
- 后续如果要继续追 `VIP / reward / 设置项` 的本地状态，不能再忽略这条桥

更关键的是，这条桥已经和已知业务状态键产生了交叉：

- `rewardTime`
  - `ref=7606`
  - `slot=0x27a2 / off=0x13d10`
  - `.text` 访问：`0x914274 / 0xa7fddc`
  - `0xa7fde8` 处明确把 `rewardTime` key 压栈后 `BL 0x9051cc`
- `favGridSortInt`
  - `ref=23817`
  - `slot=0x27a4 / off=0x13d20`
  - `.text` 访问：`0x8d672c / 0x9142ec / 0xa56cbc / 0xa65c40`
  - `0xa56cc8` 处明确把 `favGridSortInt` key 压栈后 `BL 0x9051cc`
- `验证失败`
  - `ref=14834`
  - `slot=0x2520 / off=0x12900`
  - `.text` 访问：`0x65ed78 / 0x7f8894 / 0x8a1cdc / 0xa67680 / 0xa6f8a0 / 0xa6ff58`
  - `0x8a1d10` 处会在本地状态读取后继续走 UI 链

这些调用点附近的模式也很统一：

- `0xa56cc8`: `favGridSortInt` -> `BL 0x9051cc`
- `0xa7fde8`: `rewardTime` -> `BL 0x9051cc`
- `0xa576ac`: `BL 0x904dc4`
- `0x8a1d10`: `BL 0x904dc4`
- `0xa65428`: `BL 0x905214`
- `0xa654bc`: `BL 0x905260`
- `0xa65670`: `BL 0x905180`

这意味着 `0x904dc4 / 0x905180 / 0x9051cc / 0x905214 / 0x905260` 这一簇函数，大概率就是 Dart 侧对 `shared_preferences` 的读写封装层。

当前最稳的判断是：

- `rewardTime` 已经被 **实证** 走本地持久化桥
- `favGridSortInt` 也已经被 **实证** 走同一桥
- VIP/绑定/过期状态非常可能也有一部分落在同类 `flutter.*` 键上，只是当前还没把具体 key 名继续挖出来

进一步扫 wrapper 调用点后，这条判断又变强了，因为已经能批量扫出更多同簇状态键：

- `0x9051cc`（setString-ish）一簇：
  - `rewardTime`
  - `storeIndex`
  - `enableSpeedList`
  - `modeValue`
  - `volumeTurnPage`
  - `orientationValue`
  - `detailChapterSortValue`
  - `searchSpeed`
- `0x905180`（setStringList-ish）一簇：
  - `netFail`
  - `httpProxy`
  - `gitRuleMap`
  - 多条 `Invalid internet address` 相关链
- `0x905260`（setDouble-ish / bool-config 簇）一簇：
  - `isShowHeaderCover`
  - `isSearchStringMix`
  - `enableAutoUploadBackup`
  - `isInkFilter`
  - `isNoBlank`
  - `isShowStatusBar`
  - `isDetailChapterIndexView`
  - `isNoZoom`
  - `isHalfScreen`
  - `isTwoPageSeamless`
  - `isNoLongPress`
  - `isKeepLightScreen`
  - `isBlackBackground`
  - `isAutoCheckFav`
  - `isFavChangePos`
  - `isDarkTheme`
  - `enableAloneBackup`
  - `isCheckCode`
  - `isTurnSouceUpdateFav`
  - `autoCleanBackupType`
  - `autoCleanCacheType`
- `0x904dc4`（clear/aux-ish）一簇：
  - `验证失败`
  - `isBetaUser`
  - `remoteConfigSign`
  - `gitRuleMap`

这里最有价值的新增，不是配置项本身，而是两个方向已经很明确：

1. `remoteConfigSign / netFail / Invalid internet address / 已过期！` 这些此前散落在 `0x911788 / 故障窗 / 更新` 附近的状态，现在已经能和本地持久化桥串起来。
2. `isAutoCheckFav / isFavChangePos` 这些过去被误认成“故障正文”的键，现已能明确归回“shared_preferences 设置项”这条线。
3. `0x904dc4 / 0x905180 / 0x9051cc` 这些 wrapper 的直接调用者里，已经能看到 `0x8a1d10 / 0xa576ac / 0xa7fde8 / 0xa95b8c / 0xba9dec` 等 VIP / reward / 本地状态相邻点，说明 `shared_preferences` 并不是外围配置，而是已经进入业务状态层。

所以后续如果继续攻 VIP，本轮新增的最佳方向不再只是盯着 `0xb9fc84`：

1. 继续反推 `0x904dc4 / 0x905180 / 0x9051cc / 0x905214 / 0x905260` 的确切语义
2. 以 `rewardTime` 这条已证实样本为模板，去找更多 `flutter.*` key
3. 再把这些 key 和 `0x911788 / 0xb9fc84 / 0x8ba3d0 / 0x67994c` 交叉起来

## 10. 交付与建议

### 10.1 本轮新增交付

- [analysis_round6/decode_reward_events.py](/root/ppcat_repo/analysis_round6/decode_reward_events.py)
- [analysis_round6/reward_events_decoded.txt](/root/ppcat_repo/analysis_round6/reward_events_decoded.txt)
- [analysis_round6/scan_snapshot_pool_refs.py](/root/ppcat_repo/analysis_round6/scan_snapshot_pool_refs.py)
- [analysis_round7/探索记录.md](/root/ppcat_repo/analysis_round7/探索记录.md)
- [analysis_round7/verify_pool_vip_update.py](/root/ppcat_repo/analysis_round7/verify_pool_vip_update.py)
- [analysis_round8/verify_shared_prefs_bridge.py](/root/ppcat_repo/analysis_round8/verify_shared_prefs_bridge.py)
- [analysis_round8/scan_shared_prefs_keys.py](/root/ppcat_repo/analysis_round8/scan_shared_prefs_keys.py)
- [analysis_round8/vip_shared_state_map.py](/root/ppcat_repo/analysis_round8/vip_shared_state_map.py)
- [analysis_round8/ghidra_scripts/SmokeRefs.java](/root/ppcat_repo/analysis_round8/ghidra_scripts/SmokeRefs.java)
- [analysis_round8/ghidra_scripts/VipStateProbe.java](/root/ppcat_repo/analysis_round8/ghidra_scripts/VipStateProbe.java)
- [analysis_round8/ghidra_output/ghidra_probe_summary.txt](/root/ppcat_repo/analysis_round8/ghidra_output/ghidra_probe_summary.txt)

### 10.1.0 本轮工具链补强

这一轮除了继续分析，还新落地了几项后续值得复用的工具：

- `Ghidra 12.1.2`
  - 已下载到 `analysis_round8/tools/ghidra_12.1.2_PUBLIC/`
  - 本轮已进一步打通：系统自带的是 `openjdk-21-jre-headless`，只有 runtime 没有完整 JDK，所以 Ghidra 会报 `Unable to prompt user for JDK path` / `unsupported java version`
  - 通过下载官方 Temurin `JDK 21` 到 `/root/tools/jdk21/jdk-21.0.11+10`，现已可稳定运行：
    - `support/analyzeHeadless`
    - `support/pyghidraRun`
    - `PyGhidra 3.1.0`（安装在 `/root/.config/ghidra/ghidra_12.1.2_PUBLIC/venv`）
  - 这意味着后续可以直接在本机写 Ghidra headless 脚本，不再受 GUI / JDK 卡点影响
- `rizin 0.8.2 static`
  - 已落地到 `analysis_round8/tools/bin/`
  - 已验证 `rizin / rz-bin / rz-asm / rz-gg` 可运行
  - 对整份 `libapp.so` 自动分析噪音较大，但适合补做 `BL` 调用点枚举和局部函数切片
- `androguard 4.1.4`
  - 已本机安装
  - 默认日志过于冗长，不适合全量 `AnalyzeDex`
  - 但轻量模式足够支撑定向类/字符串/方法查询，本轮已经用它确认了 `shared_preferences` 插件类与 method 集

### 10.1.0.1 Ghidra 本轮实测结论

为了确认 `Ghidra` 是否真的能服务这个样本，而不只是“装上了”，本轮额外做了两步验证：

- `analyzeHeadless` 成功 import 了 [libapp.so](/root/ppcat_repo/libapp.so)
  - loader = `Executable and Linking Format (ELF)`
  - language/compiler = `AARCH64:LE:64:v8A:default`
- 额外写了两个 headless 脚本：
  - [SmokeRefs.java](/root/ppcat_repo/analysis_round8/ghidra_scripts/SmokeRefs.java)
  - [VipStateProbe.java](/root/ppcat_repo/analysis_round8/ghidra_scripts/VipStateProbe.java)
  - 汇总日志见 [ghidra_probe_summary.txt](/root/ppcat_repo/analysis_round8/ghidra_output/ghidra_probe_summary.txt)

但轻量自动分析的实际效果也要如实记录：

- `Function Start Search` / `Create Function` 对这个 Flutter AOT `libapp.so` 的帮助有限
- 对 `0x911788 / 0xb9fc84 / 0xa54178 / 0x8a0fd4 / 0x8ba3d0 / 0x67994c`，Ghidra 默认分析仍没有直接切出函数或静态引用
- 因此，`Ghidra` 目前更适合在本项目里充当：
  - headless 脚本平台
  - ELF / 字符串 / 数据结构补充检查器
  - 后续如果继续自定义函数发现逻辑，可作为比 `capstone` 更高层的宿主

当前不建议把它当作“可直接替代 `capstone + pool_accesses + 对象池脚本` 的主战工具”。

### 10.1.1 第七轮新增进展摘要

在本轮继续深入后，`test1` 又确认了三件事：

1. **本地便携工具链已补强并验证可用**
   - `jadx 1.5.5` 已落地到 `analysis_round6/tools/jadx/`
   - `apktool 3.0.2` 已落地到 `analysis_round6/tools/apktool/apktool.jar`
   - `jadx` 已能直接对 `main_dex/classes2.dex` 的单类重新导出，例如 `p073.C1083`
2. **`unflutter` 的 `ppcat-hybrid` profile 确实命中**
   - `go run ./cmd/unflutter _debug objects --lib ../lib/arm64-v8a/libapp.so --profile ppcat-hybrid`
   - 能稳定跑过 `String/Type/TypeArguments/Code/Function/Class/Field/Library`
   - 只在 `FILL[572] CID=21 ObjectPool start=0x2e0bfb` 附近卡住
3. **官方 Dart 2.19 对照后，当前更像 pool 起点局部漂移/前导结构问题**
   - 官方 `2.19.0` 的 `ObjectPool` 仍是 `length + entry_bits + payload`
   - 合法 type 仍是标准几类，不足以解释 `0x67/0x89/0x8e`
   - 因此当前最值得继续打的是 `ppcat-hybrid` 的 `ObjectPool` 入口局部兼容，而不是换回官方 `2.19.0` profile

### 10.1.2 第七轮后续补充：test3 交叉验证已在 test1 本地独立复核

在读取 `origin/test3:分析报告6.md` 与其脚本后，我没有直接照单全收，而是把关键结论重新在 `test1` 本地跑了一遍。结论如下：

1. **对象池 raw-entry 结论已独立复核通过**
   - `fill_start = 0x2e7043`
   - `length = 51140`
   - 7 个关键 ref 在 `test1` 本地独立命中：
     - `21707 -> slot 0x59bb / off 0x2cdd8`
     - `22466 -> slot 0x9912 / off 0x4c890`
     - `26842 -> slot 0x15b1 / off 0xad88`
     - `27673 -> slot 0x276e / off 0x13b70`
     - `29112 -> slot 0x1cfd / off 0xe7e8`
     - `30922 -> slot 0x2e2d / off 0x17168`
     - `30947 -> slot 0x15b2 / off 0xad90`
   - 这意味着 `test1` 之前报告里把 `27673` 记成 `0x278d / 0x13c68` 的说法需要纠正；`0x13c68` 实际对应 `ref=11685 "isAutoCheckFav"`。
2. **`test3` 关于“真·故障 body builder”的纠偏是成立的**
   - `ref=27673` 的唯一 `.text` 访问点是 `0x921080`
   - 包围函数是 `0x920d7c`
   - `0xbbc22c` 不再应视为故障 body builder
3. **`VIP` 这条线需要重新表述，不能再把 `0xb9fc84` 当成“纯 isVip 判定小函数”**
   - `0xb9fc84` 内部同时访问：
     - `未获取` @ `0xb9fe54`
     - `未登录` @ `0xb9fe94`
     - `通过捐赠绑定设备获取` @ `0xba007c`
     - `功能，通过捐赠绑定账号获取` @ `0xba0070`
     - `点击头像绑定账号` @ `0xba027c`
     - `isDaily:` @ `0xb9fdb8`
     - `24小时内累计浏览` @ `0xba07fc`
     - `次推荐信息，即可获取` @ `0xba07d0`
     - `喵喵喵~ / 饿了喵~` @ `0xba09e8 / 0xba0bcc`
   - 它更像 **VIP/每日喂喵/账号绑定/reward 状态混合状态机**，不是单一布尔 `isVip` 开关。

### 10.1.3 VIP 内容页：目前能独立落到的精确层级

基于对象池已打通，`test1` 现在能把 VIP 内容页的核心字符串簇稳定落到以下地址：

- `0xa54178`：VIP 内容页主构造区
  - `0xa54d3c` -> `ref=8910` -> `通过捐赠绑定设备获取`
  - `0xa54e54` -> `ref=9292` -> `已获取`
  - `0xa54e80` -> `ref=29757` -> `未获得`
  - `0xa55130` -> `ref=13169` -> `功能，通过捐赠绑定账号获取`
  - `0xa555ac` -> `ref=2878` -> `24小时内累计浏览`
  - `0xa555e4` -> `ref=13199` -> `次推荐信息，即可获取`
  - `0xa5571c` -> `ref=13928` -> `，未获得特权将在0点重置浏览次数！`
  - `0xa55858` -> `ref=3524` -> `已生效`
  - `0xa55c80` -> `ref=21707` -> `每日喂喵`
  - `0xa55cbc` -> `ref=28525` -> `喵喵饿了`
  - `0xa55f30` -> `ref=21925` -> `训练喵喵`
  - `0xa55f8c` -> `ref=31475` -> `让喵喵更智能`
  - `0xa56200` -> `ref=17788` -> `每日一次`
- `0xa5796c`
  - `0xa579c0` -> `ref=10073` -> `已绑定账号`
- `0x8a0fd4`
  - `0x8a10d0` -> `ref=11366` -> `可以捐赠`
- `0x8ba3d0`
  - `0x8ba8e0` -> `ref=7406` -> `expiresDate`
- `0x67994c`
  - `0x6799e4` -> `ref=12056` -> `expires`

当前更稳的说法是：

- `0xa54178` 是 **VIP 内容页 + 每日喂喵/训练喵喵 UI 片段构造器**
- `0xb9fc84` 是 **VIP / 登录绑定 / 每日 reward 状态混合状态机**
- `0x8a0fd4` 是 **“可以捐赠”提示构造器**
- `0x8ba3d0 / 0x67994c` 是 **过期字段读取/格式化链**

但我仍没有把“单一 `isVip=true` 条件分支”收敛到可以负责任给出 patch 的程度，所以本轮不伪造一个精确 VIP patch。

### 10.1.3.1 与 `test2` / `test3` 的交叉验证

- 与 `test3` 一致：
  - 对象池 raw-entry 结果
  - `0xa54178` 与 `0xb9fc84` 不是“纯 VIP 小函数”，而是混合了 `每日喂喵 / reward / 绑定` 状态
  - `0x8ba3d0 / 0x67994c` 确实是 `expiresDate / expires` 读取链
- 对 `test2` 的纠偏：
  - `test2` 报告把每日喂喵 title/body 的访问点进一步收敛到了 `0x89afa0 / 0xb5ac34` 这套 builder 方向，但 `test1` 本轮没有复核到足以直接给 patch 的 caller 入口，所以只接受其“每日喂喵有独立 builder 线”的方向，不直接照收 patch 建议。
  - `test2` 关于“让 isVip=true 可从源头消灭广告/喂喵窗”的总体判断有启发意义，但 `test1` 目前仍认为 `0xb9fc84` 不能被负责任地压缩成一个单点 `isVip` 分支。

### 10.1.4 更新窗：比 `0x85efd4` 更稳的 patch 位点

`test3` 报告把 `0x85efd4` 作为“发现新版本”关键函数是对的，但继续往 caller 追后，`test1` 认为更稳的 patch 位点不是函数入口，而是两个上层编排器里的 dialog-show `BL`：

- `0x8667a8`
  - 原始字节：`bd33f097`
  - 目标：`BL 0x47369c`
  - 前后形态：
    - 前：`STP X2,X1,[X15,#-16]!` + `STP X4,X3,[X15,#-16]!`
    - 后：`ADD X15,#0x20` + `MOV X0,X22` + `RET`
  - 建议 patch：`1f2003d5`
- `0x8675bc`
  - 原始字节：`3830f097`
  - 目标：`BL 0x47369c`
  - 前后形态同上
  - 建议 patch：`1f2003d5`

这两个位点与已被运行时证实安全的故障窗 `BL -> NOP` 模式一致，比直接改 `0x85efd4` 入口 `return null` 更保守。

### 10.2 给主控的直接建议

优先顺序：

1. **先尝试 `0x913bf8` 两个内部 `BL` NOP**
   - `0x914e74 -> 1f2003d5`
   - `0x914f24 -> 1f2003d5`
2. Java 侧若继续做桥接恢复：
   - 不要再把 `Pangle` 当作单 `MethodChannel`
   - 以 `C0981/C0984` 现有结构为模板恢复
   - `reward` 成功必须考虑 `EventChannel` 的 event sink
   - `GDT` 需按 `C1103/C1083` 的“动态 reward 子 channel”结构单独处理
3. 对象池 / VIP 如果继续攻：
   - 对象池 raw-entry 这一层，现在 `test1` 已接受 `test3` 路线并完成本地复核
   - 重点已不再是“对象池能否解”，而是继续把 `0x911788` 这种被多路复用的状态读取函数拆成更细的语义
   - `VIP` 不建议再把 `0xb9fc84` 简化为单点 `isVip`；应分别追 `0xa54178 / 0x8a0fd4 / 0x8ba3d0 / 0x67994c`
4. 如果只想先止住更新窗：
   - 优先测 `0x8667a8 -> NOP`
   - 再测 `0x8675bc -> NOP`
   - 这两处比 `0x85efd4` 入口 return-null 更稳

### 10.3 本轮最重要的三句话

- `Pangle` 需要 **EventChannel 级别仿真**，`GDT` 需要按 **动态 reward 子 channel** 处理；通用 `MethodChannel no-op` 不能视为已完成。
- `0x913bf8` 是 **高置信候选第三故障来源**；推荐先测其两个内部 dialog `BL` 的 `NOP`。
- `test1` 现在已在本地独立复核 `test3` 的 **ObjectPool raw-entry 解析**；但 `VIP` 仍未收敛到可以负责任给出的单一 `isVip` patch，不能硬猜。
