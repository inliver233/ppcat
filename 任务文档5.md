# 第五轮代码恢复任务:两个残留广告弹窗编排器精确定位

> 纯静态分析,只读不写不修改,产出 分析报告5.md + 精确 patch 清单,提交到你的分支(test1/test2)。
> 前四轮已完成:对象池VLE解析、篡改窗patch、导入bug修复、去广告(4个SDK插件禁注册)、故障窗0xbc0f40入口patch。

## 一、背景:当前 patch 状态(libapp.so)

| 地址 | patch | 作用 | 状态 |
|---|---|---|---|
| 0x8e1dd0 | e00316aac0035fd6 (return null) | 篡改窗 | ✅ 已解决 |
| 0x8ef2b8 | e00316aac0035fd6 (return null) | 篡改窗 | ✅ 已解决 |
| 0xbc0f40 | e00316aac0035fd6 (return null) | 故障窗编排器入口 | ✅ 启动/搜索/漫画路径已挡 |
| 0xbc1020 | 00020054 (B.EQ重定向0xbc1060尾声) | 故障窗门控分支 | ✅ 双保险 |

去广告(smali):registerWith 注释掉 AdMob/Pangle/Kwad/GDT 4个插件注册。

## 二、★本轮要解决的两个残留弹窗

去广告后,广告调用 MissingPluginException,Dart 层检测到失败弹窗。**已确认两个不同弹窗,文案不同、是两个独立编排器**:

### 残留弹窗1:故障窗(偶发)
- 标题 ref=**29112** "故障"
- 正文 ref=**27673** "无法正常联网,导致广告无法显示!..."
- 共用正文 builder 0xbbc22c(全app只有0xbbc31c一处加载,已确认)
- 0xbc0f40 编排器已 patch,**但用户实测过很久后仍偶发弹一次** → 还有第二个故障窗编排器(也加载ref=27673的slot 0x13c68,通过vtable间接调度)
- VLE: ref=29112→`38 63 81`(@snapshot 0x2ece06); ref=27673→`19 58 81`(@0x2eeecb)

### 残留弹窗2:每日喂喵窗(点喵喵饿了后不断弹)★重点
- 标题 ref=**21707** "每日喂喵"
- 正文 ref=**22466** "无法加载数据,请稍后再试!\n如果开启了屏蔽广告的相关软件或插件,请关闭然后重试!\n每天喂一次喵,喵喵就不来找你了~"
- 触发:漫画页点"喵喵饿了"(激励reward广告位)→ reward调用失败(MissingPluginException)→ 弹此窗 → **每30-45秒重试反复弹**
- 这是独立的弹窗编排器(不是0xbc0f40)
- VLE: ref=21707→`4b 29 81`(@snapshot 0x2f8c82); ref=22466→`42 2f 81`(@0x305c5b)
- logcat铁证:reward失败调用=`rewardVideo on channel plugins.hetian.me/gdt_plugins` + `showRewardVideoAd on channel flutter_pangle_ads`

## 三、★核心难点:对象池 ref→slot→.text 反序列化(局部)

前几轮都没完全反序列化对象池(blutter卡在定制VM,unflutter只出ref→string)。本轮**不求完整反序列化整个pool,只求局部突破**:反序列化出"每日喂喵窗"(ref=21707/22466)和"残留故障窗"这两个弹窗相关slot的 **ref→slot→.text** 映射,从而定位它们的编排器函数入口。

已知锚点(精确,经.text验证):
- ref=27673 → slot 0x13c68 → .text 0xbbc31c(故障正文builder 0xbbc22c内)
- ref=26842 → slot 0xad88; ref=30947 → slot 0xad90
- ref=30922 → slot 0x17168 → .text 0x8e52d0/0x8ef414

需要你产出的关键映射:
- **ref=22466(每日喂喵正文)→ slot=? → 哪些.text PC加载它**
- **ref=21707(每日喂喵标题)→ slot=? → 哪些.text PC加载它**
- **ref=29112(故障标题)→ slot=? → 哪些.text PC加载它**(找残留的第二个故障窗编排器)

### 反序列化方法建议
1. 基于snapshot.json的CID表+对象池格式,写Python解析器正向反序列化cluster。重点处理type字节(0x80对象引用/0x81 double/0x00 Smi等)的不同长度,使byte→pool index精确
2. 或:对isolate snapshot data正向反序列化(cluster by cluster),输出每个pool条目的{index, slot_offset, type, 引用的ref}
3. 已有脚本:test2分支的 dump_object_pool.py / dart_parser.py / scan_pool_xref.py(但都未完全成功,可改进)
4. 验证标准:你的解析器对ref=27673必须算出slot=0x13c68(锚点)

## 四、★要产出什么

### 4.1 每日喂喵窗编排器(最高优先级)
- 反序列化出ref=21707/22466的slot
- 用pool_accesses.txt(test2有,122780条PC→slot)或capstone扫.text,找所有加载这两个slot的.text PC
- 定位"每日喂喵"弹窗的showDialog编排器函数入口.text地址
- 给出patch:让"每日喂喵"窗永不弹。参照篡改窗/故障窗手法:
  - 若编排器所有出口返回null(MOV X0,X22;RET),入口改return null(e00316aac0035fd6)——**这是已验证安全的手法(0xbc0f40就这么做的)**
  - 或找触发条件分支改跳转
- **更优方案**:定位"喵喵饿了"reward广告的成功回调路径,让reward"假装成功"(点一下就喂喵,不弹窗,免广告福利)。找reward成功onReward回调的.text,patch让它无条件触发成功

### 4.2 残留故障窗编排器
- 反序列化ref=29112(故障标题)的slot
- 找除0xbc0f40外、加载故障标题slot的.text(第二个故障窗编排器)
- 给出patch让其不弹

### 4.3 通用判据(bonus)
- 扫描全app所有"showDialog弹窗编排器"特征函数(访问弹窗标题+正文slot对、所有出口返回null的),列一个清单,看还有没有其他广告失败类弹窗会被触发

## 五、交付物(分析报告5.md,提交到你分支)

1. 对象池局部反序列化结果:ref=21707/22466/29112 的 slot + 加载它们的.text PC清单
2. 每日喂喵窗编排器.text入口 + patch方案(地址+字节)
3. 残留故障窗(第二个)编排器.text入口 + patch方案
4. (可选)reward成功回调路径 + "假装成功"方案
5. 反序列化脚本(改进版,能通过ref=27673→0x13c68锚点验证)

## 六、约束

- 纯静态。地址vaddr==file offset。.text@0x460000,snapshot data@0x3330(size 0x45c930)
- frida不可用(本机houdini),只能静态+patch-and-test
- 产物:lib/arm64-v8a/libapp.so(当前已含4处patch的版本见backup分支)+ test2的pool_accesses.txt
- 已验证patch手法:函数所有出口返回null(X22)时,入口8字节改e00316aac0035fd6安全
- 中性措辞"代码恢复"。交叉验证另一分支成果

## 七、优先级
1. 每日喂喵窗(4.1)—— 用户每次点喵喵饿了都被烦
2. 残留故障窗(4.2)—— 偶发但存在
3. reward成功方案(4.4)—— 最优解但难

关键:产出精确到本机能直接patch-and-test的地址+字节。对象池局部反序列化是钥匙,请重点投入。两份报告会交叉对比,请严谨。加油。
